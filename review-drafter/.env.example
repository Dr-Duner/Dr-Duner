# Copy to .env and fill in. The app runs without a key (shows safe
# fallback templates), so parser + compliance gate are demoable offline.
ANTHROPIC_API_KEY=
# Swap the model here — that's the only place it's referenced.
ANTHROPIC_MODEL=claude-sonnet-4-6
