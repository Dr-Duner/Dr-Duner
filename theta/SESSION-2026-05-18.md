# Session record — 2026-05-18

Focus: fixing the cross-session memory/persistence problem for Theta.

## What happened
- Identified the root cause: Claude Code only sees this git repo. Cowork,
  the simulator, and other sessions are invisible. Foundational Theta
  context was lost because it lived in Cowork, never in the repo.
- Built the persistence architecture:
  - `CLAUDE.md` — auto-loaded shared memory + rules.
  - `theta/DESIGN.md` — living spec.
  - `theta/memory/` — MEMORY.md index + project_ decision files.
  - `main` = blind-clone safety mirror, synced on every backup.
  - `.claude/settings.json` Stop hook — auto-commit + push every session.
- Adopted Gee's working protocol, adapted from unreachable Mac-local
  paths to repo-tracked paths (git is the only channel that crosses
  machines).
- Created Lesson 1 storyboard pitch and the Theta cast/arc flowchart.

## Durable decisions (also in theta/memory/)
- Repo is the single source of truth; git is the only transfer channel.
- main mirrors the working branch every backup.
- Stop hook guarantees no in-repo work is lost.

## Still open
- Cowork save location unknown (blocks Lesson 9 recovery).
- Lesson 9 artifact not in repo.
- Exact terminal font name uncaptured (decided in Cowork only).
- ~44 generation credits cap video output.

## Caveat
The Stop hook auto-activates only for sessions where
`.claude/settings.json` exists at startup — i.e. all future sessions.
This session committed manually; future sessions auto-save.
